<style>
  body { min-height: 1200px; }
  
  .fav-item {
    margin-bottom: 7px;
    text-align: center;
  }
  
  .btn-sound {
    white-space: normal;
    /*
    height: 70px;
    width: 70px;
    */
  }
  
</style>

<script type="text/javascript" src="<?php echo $this->baseUrl('/assets/js/play-button.js'); ?>"></script>
<script type="text/javascript" src="<?php echo $this->baseUrl('/assets/js/player-control.js'); ?>"></script>

<?php
  $i=0;
  $indexColor = 0;
  $colors = [
    'btn-outline-primary rounded rounded-circle',
    'btn-outline-secondary rounded-circle',
    'btn-outline-success rounded-circle',
    'btn-outline-danger rounded-circle',
    'btn-outline-info rounded-circle',
    'btn-outline-dark rounded-circle'
  ];
?>

<h2>Favorites</h2>
<div class="d-flex justify-content-between flex-wrap" id="favorites">
  <?php for ($i; $i<90; $i++): ?>
    <div class="border rounded fav-item" id="fav-<?php echo $i; ?>" ondragover="console.log(event.target);"></div>
  <?php endfor; ?>
</div>
<hr/>
<div class="d-flex justify-content-between flex-wrap">
  <?php foreach ($sounds as $theme => $files): ?>
    <?php if ($indexColor >= count($colors)) $indexColor=0; ?>
    <?php foreach($files as $filename): $i++; ?>

      <button type="button" class="m-2 btn btn-sound <?php echo $colors[$indexColor]; ?>" id="btn-<?php echo $i; ?>" onclick="playSound('<?php echo $i; ?>');" data-state="stoped" data-toggle="tooltip" title="<?php echo preg_replace(array('/.+\//', '/\-/'), array('', ' '), $filename); ?>" draggable="true">
        <?php //echo preg_replace(array('/.+\//', '/\-/'), array('', ' '), $filename); ?>
        <i class="fas fa-eject fa-rotate-90"></i>
      </button>

      <audio src="<?php echo $this->baseUrl($soundsPath.$filename); ?>" id="item-<?php echo $i; ?>">
          Your browser does not support the HTML5 Audio element.
      </audio>

    <?php endforeach; ?>
  <?php $indexColor++; endforeach; ?>
</div>

